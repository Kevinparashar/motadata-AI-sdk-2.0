# Metadata Python SDK - Production Dependencies
# Core dependencies for the SDK

# Note: Currently using only Python standard library
# Add third-party dependencies here as they are needed

# Example dependencies (uncomment when needed):
# requests>=2.28.0
# numpy>=1.24.0
# aiohttp>=3.8.0
